import styled from "@emotion/styled";
import {LEFT_COLUMN_BG_COLOR, LEFT_COLUMN_WIDTH} from "../../utils/consts.ts";

export const Wrapper = styled.div`
    width: ${LEFT_COLUMN_WIDTH};
    background: ${LEFT_COLUMN_BG_COLOR};
`;
